---
import { fade } from "astro:transitions";
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<h1 transition:animate={fade({ duration: "1s" })}>{title}</h1>

<style>
  h1 {
    font-size: clamp(50px, 5vw, 90px);
  }
</style>
